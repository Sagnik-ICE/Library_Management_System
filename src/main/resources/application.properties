# Database Configuration
db.url=jdbc:mysql://localhost:3306/library_db?useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true
db.username=root
db.password=Sagnik2314
db.driver=com.mysql.cj.jdbc.Driver

# HikariCP Configuration
hikari.pool.name=LibraryHikariPool
hikari.maximum.pool.size=10
hikari.minimum.idle=5
hikari.connection.timeout=30000
hikari.idle.timeout=600000
hikari.max.lifetime=1800000

# Application Settings
app.name=Library Management System
app.version=2.0.0
app.fine.rate.per.day=10.0
app.default.loan.period.days=14

# Logging Configuration
logging.level=INFO
logging.file.path=logs/library-management.log
logging.max.file.size=10MB
logging.max.history=30

# Security Settings
security.bcrypt.rounds=12
security.password.min.length=8
security.session.timeout=1800000

# UI Settings
ui.theme=default
ui.window.width=800
ui.window.height=600
ui.date.format=yyyy-MM-dd
